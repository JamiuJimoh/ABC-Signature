{
  "name": "abc-signature-website",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i src/input.css -o css/styles.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i src/input.css -o css/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
